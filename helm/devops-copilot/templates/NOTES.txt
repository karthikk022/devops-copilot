DevOps Copilot has been installed!

Release:    {{ .Release.Name }}
Namespace:  {{ .Release.Namespace }}
Chart:      {{ .Chart.Name }}-{{ .Chart.Version }}

Components deployed:
{{- if .Values.postgres.enabled }}
  ✓ Postgres (pgvector) at {{ include "devops-copilot.fullname" . }}-postgres:{{ .Values.postgres.service.port }}
{{- end }}
  ✓ Backend  at {{ include "devops-copilot.fullname" . }}-backend:{{ .Values.backend.service.port }}
  ✓ Frontend at {{ include "devops-copilot.fullname" . }}-frontend:{{ .Values.frontend.service.port }}
{{- if .Values.frontend.ingress.enabled }}
  ✓ Ingress: {{ (index .Values.frontend.ingress.hosts 0).host }}
{{- end }}

To access locally:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "devops-copilot.fullname" . }}-frontend 8080:{{ .Values.frontend.service.port }}
  open http://localhost:8080

Check status:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/part-of=devops-copilot
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/component=backend -f

Health check:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "devops-copilot.fullname" . }}-backend 8000:{{ .Values.backend.service.port }}
  curl http://localhost:8000/api/health

{{- if not .Values.openrouter.apiKey }}

⚠  WARNING: openrouter.apiKey is not set. Set it before the backend can serve chat:
    helm upgrade {{ .Release.Name }} helm/devops-copilot \
      --reuse-values \
      --set openrouter.apiKey=$OPENROUTER_API_KEY
{{- end }}
